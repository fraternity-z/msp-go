# 项目待办

本文是仓库唯一的当前待办清单。技术文档和历史记录只引用这里，不再维护独立路线图。

**更新时间**：2026-08-22

## 优先级说明

| 优先级 | 含义 |
|--------|------|
| P0 | 影响数据正确性、安全边界或生产验收 |
| P1 | 影响核心体验、可扩展性或维护效率 |
| P2 | 中长期产品扩展 |

## P0

### 数据正确性与认证恢复

- [x] 2026-08-11 完成三项无需数据迁移的 P0 加固：并发 401 共享单个 token 刷新 Promise，刷新或原请求重试失败时所有等待者均可结束；资源浏览量改为 PostgreSQL 原子递增并返回已提交值，资源编辑通过行锁避免覆盖并发计数，同时保留其他元数据并兼容历史非对象 `meta`；系统设置批量 UPSERT 统一置于事务中，任一设置失败即整批回滚。临时 Vitest 4 个并发与失败路径用例全部通过；临时 PostgreSQL 集成测试通过批量回滚、32 路并发查看与 32 路并发编辑、返回计数唯一性、元数据保留和旧 `meta` 归一化验证，测试源码与隔离数据均已删除；后端全量 test、vet、build 以及前端 lint、类型检查和生产构建通过。

### AI 学习闭环

- [x] 实现图片答案 OCR；保持解析成功前不创建 attempt、diagnosis、learning session 或 DKT 更新，并补应用层、HTTP、前端和真实图片验收。
- [x] 完善通用数学求解能力，覆盖超出当前本地模板和基础答案比较范围的题型，并定义可解释的失败与降级契约。
- [x] 2026-08-11 将 Tutor 从“完整生成后单次 SSE”升级为 provider 分片级端到端流式；`task_info`、多次 chunk 和持久化后的 done 逐事件刷新，断连/取消会停止生成且不发送 done，provider 部分失败会追加中断提示并保持已完成回复的分片与历史一致，流开始后不跨候选模型拼接回复。默认模型客户端复用安全 HTTP Transport 和连接池，同时保留单请求超时。临时测试覆盖正常分片、缺失尾段、部分失败、写回失败、候选切换和 64 路并发连接复用，`go test -race` 通过后已删除测试源码；全量 `go test ./...`、`go vet ./...`、`go build ./...`、前端 lint 和生产构建通过。
- [x] 2026-08-12 新增 `POST /v1/responses` OpenAI 兼容入口，复用逻辑模型、渠道优先级/权重、密钥轮换、供应商模型映射、超时、重试、AI 风控、限流和 token 指标；支持原生 Responses 成功 JSON/SSE 透传、失败脱敏及 Chat Completions fallback 的文本、拒绝、函数调用和 usage 状态机转换，流交付后不再切换渠道。学生响应成功写入下游后写共享日额度账本，账本故障不改写已交付结果；生产 Nginx 与本地 Vite 均代理 `/v1/`。不支持存储、后台任务、跨请求会话状态、内建工具、provider reusable prompt 及文件/音频输入，真实外部 provider 与官方 SDK 的兼容性仍归入生产验收。
- [x] 2026-08-14 完成无需数据库迁移的学习会话生成韧性收敛：以前端收到 `session_info`/`task_info`、或缺失元信息时收到首个 chunk/done 作为本轮接收确认；显式停止与异常中断都保存已生成部分，并分别追加精确尾注 `> 已停止生成`、`> 生成已中断`；首次聊天在停止、中断以及 claim 过期且尚无助手回复时自愈收尾，既不重复生成也不重放上一轮。生成期间可编辑下一轮文字草稿，但发送、附件、语音、模式切换和本轮操作保持锁定。取消接口校验任务所有者，等待回复落库和 AI 并发租约释放尝试完成，并以 5 分钟进程内 tombstone 支持短期重复取消；租约清理失败单独记录，不改写已经停止并落库的业务结果。任务取消表只驻留当前 Go API 进程，当前能力明确限定为单实例。临时 Go 用例覆盖并发取消、首个取消原因、租约释放顺序、首轮 claim 竞争和并发完成，并以 `go test -race -count=100` 通过；临时前端 Hook 用例覆盖接收确认及生成期下一轮草稿保留。临时测试源码删除后，后端全量 `go test ./...`、`go vet ./...`、`go build ./...`，前端 Vitest 空测试集检查、ESLint、TypeScript、生产构建及 `git diff --check` 均通过。
- [ ] 对外部 provider 的 Tutor、Portrait、Diagnostician、Math Solver、Question Parser、Question Generator 和 OCR 做真实运行质量验收。
- [ ] 将 Tutor 生成任务取消从单 API 实例内存状态扩展为多实例可用的共享取消状态，或在入口明确实施粘性路由；验证重复取消、断连、实例退出和部分回复落库的一致性。

### AI 自主练习加固

- [x] 2026-07-14 已将每学生每分钟 10 次的练习生成限流迁移到 Redis 原子计数，并保留有界进程内降级；双 API 实例共享窗口验证返回 `200,200,429`，窗口过期后恢复。生产装配通过 `exercisehttp.WithRedisRateLimit` 注入共享限流器。
- [ ] 明确练习生成限流在 Redis 运行期故障时的生产策略；当前会降级为各实例独立的本地窗口，多实例下不能保证全局每学生每分钟 10 次。选择 fail-closed、网关统一限流或其他共享故障方案，并完成故障演练。
- [ ] 定义生成题质量复核、保留期限、清理策略和错误题回算流程。
- [ ] 建立服务端可信的 Tutor 题目上下文，降低仅依赖提示词和提交后 solution 权限造成的提前泄题风险。
- [ ] 明确 AI 自主练习对个人 DKT、教师掌握度视图和班级统计的影响边界，并完成临时跨来源统计验证，记录结果后删除验证源码。
- [ ] 在配置真实 `question_generator` 模型的环境完成生成、持久化、作答、诊断和导师跳转的浏览器闭环验收。

### 生产验收

- [ ] 完成 Docker/Compose 实机 smoke，并记录镜像构建、迁移、健康检查和服务重启结果；除 frontend Nginx 固定返回 200 的 `/health` 外，必须单独验证 backend `/health/detailed`、经前端代理访问真实 API、PostgreSQL 和 Redis，避免 frontend healthy 掩盖后端依赖故障。
- [ ] 完成非 AI API、学生/教师/管理员核心浏览器流程和外部集成验收。
- [ ] 建立可重复性能基线，覆盖登录、题目列表、练习提交、学习会话和教师聚合查询；记录 P50/P95/P99、吞吐、错误率、SQL 次数、连接池等待，以及前端首屏/路由块体积预算。本次静态审计只建立了微基准和包体基线，尚未完成运行态验收。
- [x] 为 HTTP 建立按规范化路由、方法和状态类别聚合的请求计数与时延直方图，并暴露 PostgreSQL/Redis 连接池及错误指标；禁止使用用户 ID、原始 URL 或 request ID 作为 Prometheus label。2026-07-17 已保留旧总计并新增低基数 HTTP histogram、pgx/go-redis pool 指标及运维查询说明。
- [ ] 对历史纯图片答案数据进行扫描，决定是否清理或回算已受影响的 attempt、DKT state 和 profile mastery。
- [ ] 为每日一题零点预生成 worker 增加跨实例全局租约或独立调度器；当前部署只能保证仅一个 API 实例启用 `DAILY_QUESTION_PREGENERATION_ENABLED`，并验证实例切换、租约过期和重复扫描不会造成并发放大。

## P1

### 消息与协作

- [x] 2026-08-20 消息中心学生端和教师端的私信、通知、答疑及论坛左侧列表改为分页跳转展示：每页独立请求并替换列表内容，底部固定提供页码、上一页/下一页、当前页数和页码跳转，不再以“加载更多”累积历史条目；列表滚动与分页控件保持分离，避免分页随内容滚出可视区。教师端题库管理同步提供页码、上一页/下一页、当前页数与直接页码跳转，并保持服务端分页请求。
- [x] 2026-08-06 在学生和教师消息中心交付全站论坛：包含六个内部板块、贴吧式发帖、帖子与回复、附件、标签、知识点关联、搜索筛选、点赞星标收藏、最佳答案、教师精选、举报和管理员审核；普通进入先显示帖子选择界面，互动通知仍可深链目标帖；教师仅可精选自己当前所教学生的帖子，精选标记仅对发帖学生当前班级成员及该班教师可见并在列表中置顶，外班用户和管理员按普通帖子查看；管理员可筛选和处理举报、查看目标内容并将违规帖子设为不可见，也可直接永久删除任意状态帖子；管理员帖子列表默认展示全部现存帖子，并提供“可见帖子”和各状态筛选；不可见保留数据库记录供复核，管理员可通过 `POST /posts/{id}/restore` 恢复 `hidden` 帖子（已公开帖子按幂等成功处理，旧 `deleted` 状态不可恢复），`accepted_reply_id` 为空时恢复为 `open`、非空时恢复为 `resolved`，且不恢复已处理举报、已读通知或精选状态；永久删除清理多态举报并依赖数据库级联清理论坛关联行，不要求先执行不可见；不可见、恢复和永久删除在管理端列表和详情中使用带悬浮提示的图标按钮；管理员不能设置或取消精选；永久删除数据库记录不会自动删除对象存储附件，需由独立存储回收流程清理；回复、点赞、`@` 提及、最佳答案与精选统一计入消息中心未读数和消息铃铛预览。

### 学习分析体验

- [x] 将学生画像迁移到学习统计下方，提供同时间范围的匿名班级比较、带置信度门槛的优势/薄弱知识点和可执行练习入口；2026-07-30 已统一北京时间窗口与快照边界，明确全班/有效样本分母，拆分范围行为和累计 DKT 语义，并以独立行动基线和画像修订号降低练习、统计、AI 报告之间的耦合。
- [x] 将错题历史证据与复习计划拆分，提供到期、错题库和已掌握视图；2026-08-02 已交付 1/3/7 天验证、冻结题面、任务 revision、提交幂等、可重开的事务归档，以及未归档错题随时重做；正式到期复习以全权重更新 DKT，提前、已掌握、普通历史错题重做和每日题即时订正以 `0.35` 低权重更新，只有即时订正仍可在未到期时推进一次计划。2026-08-03 已修复历史每日题 JSON `null` 知识点快照导致复习查询 500 的问题，并以迁移校准、数组约束和读取降级共同加固；待复习列表只保留已到期任务，未到期任务的提前练习入口移至错题库对应题卡；无知识点的旧题目和快照已归入系统“未分类”维度，使低权重提前练习也会实际更新掌握度；错题库按学生和题目聚合，重做再次答错只累计错误次数，不再产生重复题卡，归档聚合题卡也会隐藏该题全部旧错误记录。2026-08-03 后续完整性修复又为普通班级题和 AI 题增加学生范围提交幂等，为正式复习补齐载荷摘要保护，为所有普通历史重做增加独立的学生范围 UUID 幂等绑定，对错题聚合增加部分索引、约束复习阶段/计数关系，并通过前向迁移确定性回放遗漏的 DKT 交互及同步画像派生状态。
- [x] 2026-08-04 在合并后的 `0010_mistake_review_tasks` 中校准旧本地 schema 漂移，补齐普通/复习/重做幂等列、复合归属外键、画像 JSON 约束和 DKT 知识点外键；旧摘要安全回填，并通过备份恢复、空库、脏数据和负向约束隔离验证。
- [x] 2026-08-04 在合并后的 `0010_mistake_review_tasks` 中冻结全部已提交作答的题目属性，运行时 DKT 序列不再读取可变题库；历史无快照普通作答只按迁移时最佳证据稳定化，不做无法证明的掌握度重放。
- [x] 2026-08-16 完成错题本强化（无需数据库迁移）：错题库和复习任务统一支持知识点、错误类型、到期状态、验证阶段和最低错误次数筛选，以及安全白名单排序；筛选与排序收纳到单一入口面板，筛选、排序和分页状态同步到 URL；列表题卡保持原有简略布局并统一操作图标尺寸，详情图标按需加载完整题面、作答、诊断、复习计划与历史记录，参考答案和解析默认隐藏并由题目后的按钮展开，详情滚动时标题、关闭按钮以及题目标题/难度/错误类型徽章固定在顶部。
- [ ] 保存知识点掌握度历史快照，在真实学期数据上增加起点、当前值和阶段成长趋势，并验证画像行动计划执行后的改善效果。

### 契约与验证

- [x] 2026-08-18 完成错误反馈标准化（无需数据库迁移）：HTTP 与 SSE 失败统一归一化为包含类别、业务码、状态码、可重试性、来源、请求编号和等待时间的前端 `AppError`；加载失败使用统一语义反馈与重试、刷新、关闭操作，请求编号可直接展示供排障，请求取消作为控制信号保持静默，会话流错误沿用同一边界。`429` 自动重试仅允许 `GET`、`HEAD`、`OPTIONS`，写请求不自动重放；固定窗口的 OpenAI Responses、练习生成、答案 OCR、每日一题生成和上传限流返回 `Retry-After`，前端优先遵循该值并对限流提示去重，无法可靠给出恢复时间的额度或并发限制不伪造等待时间。
- [ ] 为当前 Go API 建立可重复的 OpenAPI 导出和临时契约冻结验证流程；验证源码运行后删除，只保留导出物或运行记录。
- [ ] 使用临时 PostgreSQL Repository 集成测试验证默认值、枚举、级联、事务、聚合读模型和迁移兼容性，记录结果后删除测试源码，关闭旧 R2 状态；2026-07-17 历史基线为 Go 总 statements 58.1%、PostgreSQL adapter 12.4%。
- [ ] 对前端认证、练习、会话、题库导入、图谱转换和公共 HTTP 层分批执行临时覆盖率验证，核心业务逻辑目标 80% 以上；记录覆盖率后删除测试源码，不建立需要永久测试文件的门槛。2026-07-17 历史全仓 statements 基线为 29.64%。
- [x] 2026-07-28 删除阻碍目录统一命名的空上传遗留目录，并将 Go 服务统一到 `backend/`。
- [x] 2026-08-30 将 `go.mod`、后端 Docker builder、README 和开发指南统一为 Go 1.25.13；Go 自动工具链与 Docker Hub `golang:1.25.13-alpine` 标签可用，升级后的 `govulncheck` 未发现可达漏洞。临时 JWT 与切片工具测试分别达到 93.5% 和 100% statements，受影响包及全量 Go 测试、`go vet`、Go 构建、排除 Style 规则的 `staticcheck`、前端 lint 与生产构建通过，临时测试源码已删除。
- [ ] 补充数据库 Schema 和关键读模型说明，避免依赖超长迁移记录理解当前结构。
- [ ] 校准当前文档账本：开发指南迁移范围仍写为 `0001` 至 `0015`，实际规范迁移链已到 `0016`、后续应从 `0017` 开始；将 `backend-python-to-go-refactor.md` 明确归档或更新其中过期的 P0/P4/P6/P7 状态和 Tutor 流式待办，保持本文为唯一当前待办清单。

### 性能与可维护性治理

- [x] 2026-08-11 完成三项无需数据迁移的 P1 运行时优化：教师学生详情将授权、画像、教师范围统计、班级排名、连续学习输入和知识点计数合并为单一 CTE，并将最近练习/会话合并为有限列表查询，成功路径由 12/13 条 SQL 降至 3 条且保持响应口径；安全日志归档/删除改为 `FOR UPDATE SKIP LOCKED` 有界批次，API 启动即执行并按配置周期、超时和批次上限自动续作，支持多实例；数据库备份导入改为 `io.Reader` 流式校验、100 MB 硬上限的临时 JSONL 分表暂存和依赖序批量回放，动态列集合按原顺序使用多行 INSERT，批量失败通过可释放 savepoint 逐行回退，并在取消或结束时清理临时文件。临时应用层、worker、真实 PostgreSQL 回滚与导入批量测试通过；测试源和临时数据已删除；无 schema 或数据迁移。

- [x] 合并教师分析和学习进度概览的重复聚合查询，并限制筛选知识图谱只读取相关边；2026-07-25 已将教师总览、班级分析和学习概览分别收敛到 2、3、3 次 SQL。
- [ ] 使用生产规模固定数据集记录上述读模型优化前后的 P95、buffer hit/read 和连接池等待；开发库只完成 SQL 可执行性与稀疏数据计划验证，不替代生产基线。
- [ ] 使用真实数据和 `EXPLAIN (ANALYZE, BUFFERS)` 验证用户、知识点、题目和资源的前后通配搜索；只为确认的热点引入 `pg_trgm` 与匹配表达式索引。当前开发库数据过少，不支持建索引结论；诊断 SQL 应在生产维护窗口临时创建，验证后删除。
- [x] 将管理端 `KnowledgeGraphEditor` 和全量节点请求延迟到关系表单或图谱 tab 首次使用时加载；2026-07-25 生产构建确认图谱编辑器和 `adminGraphConfig` 为独立异步块。
- [ ] 保持现有 package 和公开契约，按用例拆分练习、教师分析和大型系统设置模块，使热点路径可以独立临时验证和基准，避免引入新的跨层抽象。

### 安全与运维

- [ ] 在生产环境复核注册策略、上传鉴权与限流、西电账户绑定验证及敏感信息不落库、管理端点网段、TLS 和安全响应头。
- [ ] 持续运行前端依赖审计与 `govulncheck`，升级后执行并记录临时兼容性验证，随后删除验证源码；历史发现以新的扫描结果为准。
- [x] 统一受保护 API 使用数据库活动账户鉴权，并以持久化 `auth_version` 撤销密码或账户状态变化前签发的全部 access/refresh token；2026-08-10 已覆盖自助改密、管理员重置、重置审批、账户停用与前端重新登录流程。
- [ ] 为允许访问私网的 OpenAI-compatible 网关、私有对象存储或校园代理设计显式 allow-list 和审计机制，避免放宽默认出站保护。
- [ ] 将登录锁定、验证码和一次性 refresh session 等安全状态与 `allkeys-lru` 普通缓存隔离，或为共享 Redis 建立淘汰、容量和多实例一致性压测，确保缓存满载不会缩短安全状态生命周期。

## P2

- [ ] 对接 LTI/OneRoster 或其他教务标准。
- [ ] 开展小规模灰度试点并建立反馈指标。
- [ ] 扩展线性代数、概率论等学科内容。
- [ ] 基于真实学习反馈优化 Agent 策略和推荐效果。
- [ ] 评估移动端应用或进一步的响应式体验优化。

## 完成规则

1. 完成项必须附带代码和运行记录，不能只更新勾选状态；临时测试需记录命令与结果后删除源码。
2. 架构、开发、部署、数据库迁移或待办发生变化时，更新对应的当前文档，不维护重复路线图。
3. 已完成且需要保留追溯信息的专项方案或报告移入 [历史归档](archive/README.md)。
